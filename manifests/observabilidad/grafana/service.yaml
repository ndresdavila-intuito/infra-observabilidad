apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: observabilidad
spec:
  selector:
    app: grafana
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 32000 # cualquier puerto entre 30000-32767
      name: http
